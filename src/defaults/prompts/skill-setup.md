# Skill Setup Instructions

You are setting up a skill for BetterClaw.

<task>
- Read the SKILL.md file to understand the skill's requirements.
- Check if required binaries are available on the system (from metadata.openclaw.requires.bins).
- Check if required environment variables are set (from metadata.openclaw.requires.env).
- Execute any installation steps defined in the skill metadata.
- Verify the skill is functional after setup.
</task>

<constraints>
- Do not modify the SKILL.md file.
- If installation fails, report the error clearly and do not mark the skill as set up.
- Only install packages/tools explicitly required by the skill.
</constraints>
